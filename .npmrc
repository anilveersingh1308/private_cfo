# Suppress non-critical deprecation warnings that don't affect deployment
legacy-peer-deps=false
engine-strict=false

# NPM will still warn but won't fail on these
# suppress-deprecation-warnings=true
